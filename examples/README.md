# Examples

- **`notes-v2/`** — the current v2 fixture: a minimal but complete
  `specs/001-core/PLAN.md` (all 8 sections, KJ-/R-/U-IDs, no risk modules
  triggered) plus a `.brana/ledger.md` mid-cycle snapshot. Passes
  `tools/brana-gate docs`. Use it as the shape reference for `brana-plan`
  output and the ledger protocol.
- **`dally/`** — **archived v1 example.** Built with the retired seven-phase
  workflow (brana-1…7, SPEC/TASKS artifacts, gate tasks). Kept as history;
  do not use it as a template — its PROMPTS.md invokes skills that no longer
  exist and its artifacts follow the v1 schema.
