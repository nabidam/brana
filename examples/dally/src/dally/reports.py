"""Report math — period resolution (ISO week / month / year) and averages.

Scaffold stub (task 0): ``Report``, ``Bucket``, and ``build_report`` land in
task 6. Pure functions — ``build_report`` takes ``today`` explicitly and never
touches the clock, prints, or exits (ARCHITECTURE.md §Decision log).
"""

from __future__ import annotations
