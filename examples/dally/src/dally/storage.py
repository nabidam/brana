"""Persistence layer — sqlite3 connection, schema init, entry CRUD.

Scaffold stub (task 0): the connection helper, ``Entry`` dataclass, and the
query functions land in task 1. This module raises on invalid input and never
prints or exits (ARCHITECTURE.md §Error handling).
"""

from __future__ import annotations
